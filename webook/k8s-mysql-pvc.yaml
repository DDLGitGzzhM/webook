apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: webook-mysql-live-claim
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi # 一个 G
